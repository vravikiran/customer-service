package com.app.service.customer.enums;

/**
 * Enum for CSV file headers
 */
public enum CustomerCSVFileHeaders {
	parentCompany, dataimportfk, customerType, customerCode, customerName, customerAlias, brand, supplyState, gstType,
	istaxexempt, greeting, creditStatus, rating, allowDuplicateGSTIN, customerGstIn, phoneno, mobileno, faxnumber,
	supplyGstIn, panno, email, website, tanno,SlNo, Duplicate;
}
